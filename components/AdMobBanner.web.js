import React from "react";

/**
 * Web placeholder: AdMob is native-only. This file is used when building for web
 * so the native module is never imported and the web build succeeds.
 */
export default function AdMobBanner() {
  return null;
}
